
labels: {}
  # tier: infra
  # feature: ambassador

replicaCount: 1

image:
  repository: tomwganem/ambassador-auth-jwt
  tag: '0.2.0'
  pullPolicy: IfNotPresent
  # pullSecret:

service:
  enabled: true
  type: ClusterIP
  # Configuration that will be added to the annotation
  config: {}
      # path_prefix:  "/auth"
      # proto: http
      # allow_request_body: false

sentry:
  environment: 'dev'
  dsn: ''

jwt:
  issuer: ''
  outbound_header: x-session-token
  check_exp: true

resources: {}
nodeSelector: {}
affinity: {}
tolerations: {}
