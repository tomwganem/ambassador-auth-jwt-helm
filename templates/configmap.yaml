---
apiVersion: v1
kind: ConfigMap
metadata:
  metadata:
  name: {{ template "ambassador-jwt-auth.name" . }}
  labels:
    app: {{ template "ambassador-jwt-auth.name" . }}
    chart: {{ template "ambassador-jwt-auth.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  SENTRY_CURRENT_ENV: {{ .Values.sentry.environment | quote}}
  SENTRY_DSN: {{ .Values.sentry.dsn | quote }}
  JWT_ISSUER: {{ .Values.jwt.issuer | quote }}
  JWT_OUTBOUND_HEADER: {{ .Values.jwt.outbound_header | quote }}
  CHECK_EXP: {{ .Values.jwt.check_exp | quote}}
